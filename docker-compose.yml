services:
  python-api:
    build: ./python
    ports:
      - "8000:8000"
    volumes:
      - ./python/app:/app/app
      - chroma-data:/app/chroma_db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
  
volumes:
  chroma-data: